import { Card } from '@/components/ui/card';

const UniversityAcceptanceLetters = () => (
  <div className="space-y-4">
    <div>
      <h1 className="text-2xl font-semibold tracking-tight">Acceptance Letters</h1>
      <p className="text-muted-foreground">Generate, review, and send offer packets to admitted students.</p>
    </div>
    <Card className="p-6 text-sm text-muted-foreground">
      Letter templates and approval flows will live here once the workflow is finalised.
    </Card>
  </div>
);

export default UniversityAcceptanceLetters;
