import { Card } from '@/components/ui/card';

const UniversityEvents = () => (
  <div className="space-y-4">
    <div>
      <h1 className="text-2xl font-semibold tracking-tight">Events & Webinars</h1>
      <p className="text-muted-foreground">Track upcoming activities, registrations, and follow-ups.</p>
    </div>
    <Card className="p-6 text-sm text-muted-foreground">
      Event scheduling and attendee dashboards will be integrated here in a later milestone.
    </Card>
  </div>
);

export default UniversityEvents;
