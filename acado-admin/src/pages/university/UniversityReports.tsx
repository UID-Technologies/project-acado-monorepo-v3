import { Card } from '@/components/ui/card';

const UniversityReports = () => (
  <div className="space-y-4">
    <div>
      <h1 className="text-2xl font-semibold tracking-tight">Reports</h1>
      <p className="text-muted-foreground">Export snapshots and share insights with stakeholders.</p>
    </div>
    <Card className="p-6 text-sm text-muted-foreground">
      Scheduled and on-demand reporting modules will appear here soon.
    </Card>
  </div>
);

export default UniversityReports;
